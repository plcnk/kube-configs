global:
    image:
        repository: ghcr.io/goauthentik/server
        tag: 2024.6.1

authentik:
    secret_key: "2fkyKhai9KXpy66c2rP7JNvgDjiAeRXq9gyNcy5hhWXgh9FA9v"
    # This sends anonymous usage-data, stack traces on errors and
    # performance data to sentry.io, and is fully opt-in
    error_reporting:
        enabled: false
    postgresql:
        password: "w3EsGc6bgvJC8dNfDZ9Co2vEmT4xWh"

server:
    ingress:
        # Specify kubernetes ingress controller class name
        ingressClassName: nginx
        enabled: true
        annotations:
            kubernetes.io/ingress.class: nginx
        hosts:
            - auth.lab.intra
        paths:
            - /
        pathType: Prefix
        tls:
            - secretName: authentik-tls
              hosts:
                  - auth.lab.intra

postgresql:
    enabled: true
    auth:
        # Password for the "postgres" admin user
        postgresPassword: "n8Vx7VJpbwuoCD2RojqarLOvtc3Z0K"
        # Password for the database user. Default username "authentik"
        password: "w3EsGc6bgvJC8dNfDZ9Co2vEmT4xWh"
    image:
        registry: docker.io
        repository: bitnami/postgresql
        tag: 15.7.0-debian-12-r18
redis:
    enabled: true
    image:
        registry: docker.io
        repository: bitnami/redis
        tag: 7.2.5-debian-12-r3
